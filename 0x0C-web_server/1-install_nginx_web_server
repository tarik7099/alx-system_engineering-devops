#!/usr/bin/env bash
# Bash script that installs Nginx server
#update packages

sudo apt-get -y update
sudo apt-get -y install nginx
#configration
sudo ufw allow 'Ngin HTTP'
#create a index
echo "Hello World!" |sudo tee /var/www/html/index.html
#test
sudo nginx -t
#restart
sudo service nginx restart
